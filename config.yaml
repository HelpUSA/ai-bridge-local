# AI Bridge Local - Configuração
# Versão: 0.1.0

gateway:
  host: "127.0.0.1"
  port: 8766

worker:
  poll_interval_seconds: 2
  max_retries: 3

queue:
  db_path: "queue_local.db"
  max_stale_minutes: 30

logging:
  level: "INFO"
  file: "gateway.log"
